# ── Rust build output ──────────────────────────────────────────────────────────
# Cargo.lock is intentionally tracked — this is a binary application.
# Committing it ensures reproducible builds for all contributors.
target/

# ── Runtime-generated files ────────────────────────────────────────────────────
current_song.json
**/current_song.json
settings.json
.osmv.lock
debugomatic.log

# ── OS files ───────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── IDE / editor ───────────────────────────────────────────────────────────────
.vscode/
.idea/
.vs/
*.swp
*.swo
*~

# ── Windows build leftovers (kept for reference but never rebuilt) ─────────────
windows/build/
windows/bin/
windows/obj/
windows/current_song.json
windows/build_log.txt

# ── Linux build leftovers ──────────────────────────────────────────────────────
linux/dist/
linux/build/
linux/.venv/

# ── Misc ───────────────────────────────────────────────────────────────────────
*.zip
test/